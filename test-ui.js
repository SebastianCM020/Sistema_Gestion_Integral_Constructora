const puppeteer = require('puppeteer'); (async () => { try { const browser = await puppeteer.launch({ args: ['--no-sandbox', '--disable-setuid-sandbox'] }); const page = await browser.newPage(); page.on('console', msg => console.log('BROWSER LOG:', msg.text())); page.on('pageerror', error => console.log('BROWSER ERROR:', error.message)); await page.goto('http://localhost:5173'); await page.type('input[type=email]', 'residente@icaro.dev'); await page.type('input[type=password]', 'Icaro2025!'); await page.click('button[type=submit]'); await page.waitForNavigation(); await page.goto('http://localhost:5173/obra/consumos'); await new Promise(r => setTimeout(r, 2000)); await browser.close(); console.log('Test finished'); } catch(e) { console.error('TEST ERROR:', e); } })();
