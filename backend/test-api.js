const { PrismaClient } = require('@prisma/client'); const jwt = require('jsonwebtoken'); const p = new PrismaClient(); async function test() { try { const u = await p.usuario.findFirst({ where: { email: 'residente@icaro.com' }, include: { rol: true } }); if (!u) return console.log('NO USER'); const t = jwt.sign({ id: u.id, email: u.email, rol: u.rol.nombre, idRol: u.idRol }, process.env.JWT_SECRET || 'icaro_secret_key_2026_secure', { expiresIn: '1d' }); const as = await p.asignacionProyectoUsuario.findFirst({ where: { idUsuario: u.id } }); if (!as) return console.log('NO ASIGNACION'); const r = await fetch('http://localhost:3001/api/v1/consumo/proyectos/' + as.idProyecto + '/materiales-disponibles', { headers: { Authorization: 'Bearer ' + t } }); const data = await r.json(); console.log('API_RES:', JSON.stringify(data, null, 2)); } catch(e) { console.error('ERROR:', e.message); } finally { p.$disconnect(); } } test();
